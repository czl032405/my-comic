{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "dev:server": "lerna run --scope *server  dev --parallel",
    "dev:serverless": "lerna run --scope *serverless  dev --parallel",
    "build:server": "lerna run --scope *server  dev --parallel",
    "build:serverless": "lerna run --scope *serverless  dev --parallel",
    "build": "lerna run build --parallel"
  }
}
